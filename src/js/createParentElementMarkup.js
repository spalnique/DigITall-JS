// function createParentMarkup(childElementMarkup, n) {
// where n is a number of child elements to be added to parent element markup //
// check MDN knowledgebase for Array.from() or feel free to ask your colleagues for help //

//     const children = Array.from({ length: n }, () => childElementMarkup);

//     const childrenElementMarkup = children.join('');

//     const parentElementMarkup = `<div class='parent'>${childrenElementMarkup}</div>`;

//     return parentElementMarkup;
// }

// function createParentMarkup(childElementMarkup, n) {
//   const childrenElementMarkup = childElementMarkup.join('');
//   const parentElementMarkup = `<div class='parent'>${childrenElementMarkup.repeat(
//     n
//   )}</div>`;

//   return parentElementMarkup;
// }
